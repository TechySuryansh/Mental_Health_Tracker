import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allowed origins for accessing Next.js dev resources (like Hot Module Replacement) over a network
  allowedDevOrigins: ['10.7.15.194', '127.0.0.1', 'localhost'],
  /* config options here */
};

export default nextConfig;
